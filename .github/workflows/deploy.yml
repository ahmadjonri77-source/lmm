on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy over SSH and run Docker Compose
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            echo "Creating project folder if it doesn't exist!"
            mkdir -p /home/ubuntu
            cd /home/ubuntu

            if [ ! -d "lmn_backend/.git" ]; then
              echo "Cloning repository!" 
              git clone https://github.com/ahmadjonri77-source/lmn_backend
            else
              echo "Pulling latest changes!"
              cd lmn_backend   
              git pull origin main  
            fi

            cd /home/ubuntu/lmn_backend
  
            echo "Stopping containers"
            sudo docker compose down

            echo "Building containers"
            sudo docker compose build 

            echo "Running containers"
            sudo docker compose up -d 